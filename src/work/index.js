import { getCategories, addCategory, editCategory, deleteCategory} from './categories';

import { getPosts, addPost, editPost, deletePost } from './posts';

export {
	
      getCategories,
      addCategory,
      editCategory,
      deleteCategory,

      getPosts,
      addPost,
      editPost,
      deletePost
                  };
